services:
  db:
    image: postgres
    environment:
      POSTGRES_USER: "test_user"
      POSTGRES_PASSWORD: "test_pass"
      POSTGRES_DB: "books_db"
  app:
    restart: on-failure
    image: my-python-db
    depends_on:
      - db
    volumes:
      - .:/app